<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_item"
        android:title="gsdf"
        app:showAsAction="ifRoom"
        app:actionLayout="@layout/menu_action_view"/>

    <item
        android:id="@+id/left_item"
        android:icon="@drawable/dots"
        android:title="Menu"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/menu_show_profile"
                android:title="Xem hồ sơ" />
            <item
                android:id="@+id/menu_watch_order"
                android:title="Xem lịch sử đặt hàng" />
            <item
                android:id="@+id/menu_cart"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:title="Giỏ hàng" />
            <item
                android:id="@+id/menu_log_out"
                android:title="Đăng xuất" />
        </menu>
    </item>

</menu>
